import { FilterByMilkywayPipe } from './filter-by-milkyway.pipe';

describe('FilterByMilkywayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMilkywayPipe();
    expect(pipe).toBeTruthy();
  });
});
