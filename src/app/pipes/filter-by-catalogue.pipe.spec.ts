import { FilterByCataloguePipe } from './filter-by-catalogue.pipe';

describe('FilterByCataloguePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCataloguePipe();
    expect(pipe).toBeTruthy();
  });
});
