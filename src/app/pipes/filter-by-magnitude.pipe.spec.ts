import { FilterByMagnitudePipe } from './filter-by-magnitude.pipe';

describe('FilterByMagnitudePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMagnitudePipe();
    expect(pipe).toBeTruthy();
  });
});
